<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/page_background"
    android:fillViewport="true" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="0dp" >

        <!-- TypeSmart logo -->

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:padding="10dp"
            android:scaleType="fitCenter"
            android:src="@drawable/title_logo" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:paddingBottom="2dp"
            android:paddingLeft="0dp"
            android:paddingRight="5dp"
            android:paddingTop="2dp"
            android:text="@string/installer_title"
            android:textSize="@dimen/heading_font_size" />

        <Button
            android:id="@+id/btn_installer_enable"
            style="@style/btn_installer"
            android:text="@string/installer_label_enable" />

        <Button
            android:id="@+id/btn_installer_default"
            style="@style/btn_installer"
            android:text="@string/installer_label_set_default" />

        <Button
            android:id="@+id/btn_installer_language"
            style="@style/btn_installer"
            android:text="@string/installer_label_language" />

        <Button
            android:id="@+id/btn_installer_keyboard"
            style="@style/btn_installer"
            android:text="@string/installer_label_keyboard" />

        <Button
            android:id="@+id/btn_installer_currency"
            style="@style/btn_installer"
            android:text="@string/installer_label_currency" />

        <!-- Comet logo -->

        <include layout="@layout/comet_logo" />
    </LinearLayout>

</ScrollView>